import { Fragment } from "react"

const Footer = ( ) => {
    return(
        <Fragment>
        <footer className="bg-blue-300 py-4 w-full text-center text-sm fixed bottom-0 flex">
                footer
            </footer>
        </Fragment>
    )
}

export default Footer;