import { Fragment } from "react"

const Header = ( ) => {
    return(
        <Fragment>
            <header>
            <h1>Header</h1>
            </header>
        </Fragment>
    )
}

export default Header;